# UI, API, and Product Design Coverage Map

## Scope
This document maps implemented backend APIs to current UI coverage and product design intent.
Canonical hierarchy and update rules are defined in `backend/docs/governance/documentation-source-of-truth.md`.
Detailed requirement-to-API-to-UI verification evidence is tracked in `backend/docs/governance/e2e-requirement-api-ui-verification.md`.

## Legend
- `Full`: UI supports primary operator workflow for the endpoint set.
- `Partial`: UI supports read-only or subset workflow.
- `Gap`: API exists but has no UI workflow yet.

## Domain Coverage Matrix
| Domain | API Prefixes | Product Intent | UI Coverage | Status | Gaps |
|---|---|---|---|---|---|
| Agent Registry & Ownership | `/agents/*`, `/owners/*` | Register agents, transfer ownership, inspect history | Register tab with catalog-backed type options (`GET /agents/register-options`), optional bootstrap config via Configuration Studio APIs, plus ownership list + transfer + history in Agents view | Full | None for core CRUD/ownership operations |
| Agent Runtime Config | `/agent-configs/*` | Configure per-agent runtime and fallback policy | Agent Configuration Studio (save/list/edit/delete/import/export) | Full | Bulk server-side import/export endpoint not present (UI handles local file workflows) |
| Global Runtime Config | `/runtime-config/*` | Tune non-startup defaults | Runtime Config Studio (save/list/edit/delete presets, database-backed rule catalog with Super Admin CRUD) | Full | None for core operator workflows |
| Discovery | `/discovery/*` | Source sync, live connections, triage conflicts/alerts/duplicates, merge/dedup, promote | Discovery console covers posture score ring, confidence distribution, auto-refresh, clickable KPI metrics, horizontal-scroll topology map with per-node subtext, **Agent Ops & Trace Sources** card (Observability pivot), agent-ops functional labels (not vendor names), agent detail drawer, agents CSV export, `/discovery/summary` dashboard, source sync, connections, duplicate/triage workflows, and resolve/promote actions | Full | Bulk merge across multiple groups and dedicated connection detail panel still absent |
| Cost Governance | `/cost/*` | Spend telemetry, budgets, policy/limit eval, anomalies | Cost console covers live telemetry (including recent session/agent drilldown pickers), request-tagged spend tracking ingestion, budget policy lifecycle with window types **realtime**, **daily**, **adhoc**, **weekly**, and **monthly** (timezone reset windows, temporary increases, soft alerts, rate/session caps, effective budget on list responses), historical spend projection on policy/limit evaluation (blended linear extrapolation + prior-period averages), pricing catalog + calculator workflows, ranked model intelligence browsing, anomaly review, session/agent cost drilldowns, Telemetry **Spend timeseries** via `GET /cost/timeseries`, and **Scope breakdown** via `GET /cost/breakdown` | Full | None for core operator workflows |
| Audit | `/audit/events` | Evidence and action traceability | Recent events list with action-prefix, resource-type, outcome, and time-window filters; context column surfaces run/trace metadata | Full | Deep compliance bundle filters remain on Compliance console |
| Compliance | `/compliance/*` | Controls mapping, evidence, retention/legal hold lifecycle | Compliance console covers control coverage with route-level drill-down, evidence generation, evidence freshness, mapping CRUD, retention policies, legal hold actions, evidence bundle drill-down (artifact inventory, integrity status, latest artifact, and event rows), a CISO investigation drawer with one-click pivots to trace/log/audit workflows, and **server-side export** via `POST /compliance/evidence/export` with optional investigation context embed (fail-closed on server error; deny audit on missing control) | Full | None for core operator workflows |
| Browser Security (GuardBridge) | `/browser/*` | Govern browser-side AI interactions with minimal telemetry, shadow AI visibility, and risk policy enforcement | Browser Security console covers event/session review, shadow AI inventory triage, risk policy CRUD, summary/analytics dashboards, and incident evidence export workflows | Full | Browser extension packaging and store-release automation remain an operational follow-up |
| Auth & Session Governance | `/auth/*` | Session policy governance, SSO/SCIM, break-glass auth | Security view now covers session policy read/update/revisions/rollback, SSO provider lifecycle (create/update/test/sync), role-binding validation plus explainability matrix and evidence drilldown, explicit auth authorization explain simulation, governed session issue/get/reauth, break-glass basic-auth lifecycle, and directory operations | Partial | Advanced policy what-if simulation depth can still be expanded |
| Gateway & Keys | `/gateway/*`, `/keys/*`, `/v1/*` | Route policies, optimization, key lifecycle, key guardrail policy controls, MCP tool gateway workflows, memory/context workflows (`/gateway/memory/*`), and API-first OpenAI compatibility surfaces | Routing & Gateway now covers route create/list, adaptive/lowest-cost/lowest-latency/least-busy strategy selection, scoped gateway entitlement list/upsert workflows, NHI inventory/hygiene readback workflows, access-review campaign create/read workflows, JIT access request create/approve workflows, least-privilege recommendation read/apply workflows, gateway governance evidence aggregation/export via dedicated backend endpoint (`POST /gateway/governance/evidence/export`) for CISO/security review bundles, dedicated **Memory & Context** tab (`GET /gateway/memory/overview`, memory record CRUD with 16 KiB content cap, per-scope limits, short-term TTL, Agent Owner scoping, production long-term dual-approval on create/delete, checkpoint/realtime links, system-rules/responses shortcuts), dedicated OpenAI-compatible operator workflows for `/v1/chat/completions`, `/v1/respo
... [truncated]
